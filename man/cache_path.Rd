% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache_path.R
\name{cache_path}
\alias{cache_path}
\title{Construct a standardized cache file path for model outputs}
\usage{
cache_path(mut, what, ext = c("rds", "parquet", "rds.gz"))
}
\arguments{
\item{mut}{Character string specifying the mutation identifier.
Any colons, slashes, or spaces are replaced with underscores
to ensure filesystem-safe filenames.}

\item{what}{Character string describing the contents of the cache
(e.g. \code{"full_model_output"}, \code{"posterior_draws"},
\code{"optimized_ell_tau2"}).}

\item{ext}{Character string specifying the file extension.
Must be one of \code{"rds"} or \code{"parquet"}.}
}
\value{
A character string giving the full path to the cache file.
}
\description{
Generates a consistent filename for cached model outputs based on a
mutation identifier, a descriptive cache key, and a file extension.
The resulting path is placed inside the global cache directory
\code{CACHE_DIR}.
}
\details{
This helper is intended to ensure reproducible and collision-free
file naming for saved model results, intermediate outputs, or
derived data products.


The filename is constructed using the template:

\preformatted{
<mutation>_<what>.<ext>
}

where \code{<mutation>} has unsafe characters replaced by underscores.

The function assumes that a global variable \code{CACHE_DIR} exists
and points to a writable directory. The directory itself is not
created by this function.
}
\note{
For safer reuse inside packages, consider passing \code{CACHE_DIR}
explicitly as an argument rather than relying on a global variable.
}
\examples{
\dontrun{
CACHE_DIR <- "R_ignore/R_scripts/outputs"

cache_path(
  mut  = "k13:561:H",
  what = "full_model_output",
  ext  = "rds"
)
}

}
